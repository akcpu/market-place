version: '3'
services:
  app:
    container_name: "Market-Place-auth"
    restart: always
    build: .
    ports:
      - '80:3002'
    external_links:
      - mongo
    env_file:
      - ./.env
  mongo:
    container_name: mongo
    image: mongo
    ports:
      - '27017:27017'
